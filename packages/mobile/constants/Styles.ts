import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
  hstack: {
    flexDirection: "row",
    alignItems: "center",
  },
  vstack: {
    flexDirection: "column",
  },
  divider: {
    width: "100%",
    minHeight: 1,
  },
});
